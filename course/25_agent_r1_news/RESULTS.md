# 实测结果记录

本页只记录真实执行过的结果，并明确区分确定性流水线、冒烟训练和正式训练。`outputs/` 不提交 Git，因此这里是可持久化、可上传的摘要。

## 实验环境

| 项目 | 本次实测环境 |
|---|---|
| 基础模型 | `Qwen3.5-0.8B-Base`，852,985,920 参数 |
| ms-swift | 官方 Git 标签 `v4.4.3`，提交 `e1287928be4451b9ed5e2fb00a24ad3c8f61287b` |
| ms-swift 运行时字符串 | `4.5.0.dev0`；官方 `v4.4.3` 标签中的 `swift/version.py` 原样如此，不是本课程改号 |
| Python / PyTorch | Python 3.12.13；PyTorch `2.11.0+gitd0c8b1f`，HIP 7.2.53211 |
| 训练栈 | Transformers 5.12.1、TRL 0.29.1、PEFT 0.19.1、Accelerate 1.14.0、ModelScope 1.39.0 |
| 推理引擎 | vLLM `0.26.0+rocm723` |
| GPU | 1 × AMD MI308X，`gfx942`，191.69 GiB HBM |
| 系统 | Ubuntu 22.04.5 LTS，ROCm 驱动 6.10.5 |

训练框架版本应以官方标签和提交为准，不能只看运行时字符串。完整的持久化环境、重建方法与 AMD 注意事项见仓库根目录的 [训练环境笔记](../../TRAINING_ENVIRONMENT.md)。

## 确定性流水线

全部 320 条验证新闻、51 条物理规则：

| 方法 | Recall@5 | Recall@10 | MRR |
|---|---:|---:|---:|
| BM25 | 0.433 | 0.578 | 0.558 |
| 哈希稠密基线 | 0.105 | 0.140 | 0.160 |
| Hybrid | 0.313 | 0.398 | 0.485 |
| Hybrid + Rerank | **0.498** | **0.597** | **0.632** |

决策准确率：无知识关键词基线 0.784，全量知识关键词基线 0.831，RAG Top-1 0.856，未经选择的 Top-8 加机械 Composer 0.788。结果说明重排有效，但把候选全部组合会引入噪声。

SFT 聊天模板长度审计：三个任务最大长度分别为 retrieve 2435、compose 2916、decision 2832 token，全部低于 4608，没有静默删除超长样本。

规则库结构审计：51 条物理规则对应 47 条 canonical 规则，包含 4 个新旧版本组；四个新版 ROOT 规则优先级均为 100，旧版均为 25。24 条规则带成立条件，26 条带例外；ID、类别、状态、来源、字段类型和优先级范围错误均为 0。

数据泄漏审计：2880 条训练任务样本来自 960 篇独立新闻，960 条验证任务样本来自 320 篇独立新闻；正文 SHA-256 和新闻 `record_id` 的训练/验证交集均为 0。每个新闻 `record_id` 恰好复用三次，分别对应 retrieve、compose、decision，没有缺失或重复展开。

专家轨迹审计曾发现一篇新闻的 gold 证据只有单字“党”，不满足环境至少两个字符的检索约束，导致对应三条 SFT 轨迹重复反思并在最大轮数停止。生成器现会为单字证据补入类别和 ROOT 规则关键词；重新生成后，2880 条训练轨迹与 960 条验证轨迹全部以合法 `finish` 结束。把 3840 条轨迹的 12800 个动作逐轮送回真实环境后，system prompt、观察、工具结果与结束状态全部一致，回放错误为 0。修复只改变这三条训练 SFT 轨迹，RL、验证和 smoke 数据均保持不变。

在干净工作区以相同源数据、种子和参数再次运行 `prepare_data.py` 后，Git 差异为 0，说明规则库、SFT、RL 和校验和文件均可逐字确定性重建。

本页已经记录的正式 SFT adapter 及其后续 GRPO 长跑在该审计修复前启动，因此起始 adapter 见过上述三条旧轨迹，占 SFT 训练集的 0.104%；GRPO 的 prompt-only RL 文件没有变化。现有结果保留这项可追溯限制，不追写成修复后重跑结果；从仓库重新执行课程时会自动使用已修复的完整轨迹。

## 冒烟训练

### SFT 三步

- 训练 loss：1.422。
- 验证 loss：1.116。
- 验证 token accuracy：0.801。
- 峰值 HBM：55.6 GiB。
- 结论：训练和保存链路通过；真实三任务各一条的轨迹评测仍失败，不能视为有效模型。

### 早期 GRPO 两步

- 两个步骤均完成多轮 vLLM rollout、六路奖励、反向传播和保存。
- 单步约 22 秒，峰值 HBM 161.7 GiB。
- 第二步 reward 均值 -0.139、reward std 0.413，说明组内已有非零训练信号。
- 负奖励来自仅三步 SFT 的策略经常违反工具顺序或输出无效 JSON，符合预期。

### GRPO 显存与吞吐探索

| 配置 | 最大轮数 | 结果 | 观察 |
|---|---|---:|---:|
| batch 8、G=2、无梯度检查点 | 4 | 2/2 成功 | 每步 4 条独立 prompt，但 G=2 零方差偏多 |
| batch 10 或 12、G=2 | 4 | 首次 backward OOM | rollout 空闲显存不能代表 backward 安全 |
| batch 8、G=2、无梯度检查点 | 6 | 第二次 backward OOM | 恢复轮次使随机轨迹变长，原边界不再安全 |
| batch 6、G=3、无梯度检查点 | 6 | 偶发第三次 backward OOM | 降低 vLLM 比例仍不能消除训练激活峰值 |
| batch 6、G=3、梯度检查点、生成批次 12 | 6 | 8/8 成功 | 峰值逻辑显存 161.2 GiB，总运行 93 秒 |

最终配置一次 rollout 生成 12 条轨迹，供两个训练 step 复用。日志中有完整 reward 的 rollout step 平均约 19.4 秒，但把中间复用 step 一起计入后，端到端平均为 11.6 秒/step。8 步的平均 reward std 为 0.152，零方差组占比 0.438，说明 G=3 保留了可用但仍不充足的比较信号。训练器的 `memory(GiB)` 会把共享的 PyTorch/vLLM 账户相加，属于逻辑峰值，不等同于 `rocm-smi` 物理占用。五个自定义奖励与 gym 过程奖励均实际参与计算。

## 旧版共享提示词 SFT：失败对照

使用 2880 条全量专家轨迹、batch 8、LoRA rank 32 训练 3 轮，共 1080 step：

| 轮次 | checkpoint | eval loss | eval token accuracy |
|---:|---:|---:|---:|
| 1 | 360 | 0.02349 | 0.99222 |
| 2 | 720 | **0.02207** | 0.99396 |
| 3 | 1080 | 0.02366 | **0.99457** |

- 总运行时间：64 分 25 秒。
- 峰值 HBM：190.23 GiB，设备物理容量为 191.69 GiB。
- 第三轮 token accuracy 继续上升，但 eval loss 反弹，已出现轻微过拟合迹象。

### 旧版 120 条真实动态轨迹

每个任务 40 条，使用同一验证子集与确定性生成：

| checkpoint | retrieve F1 | compose F1 | decision 组合 F1 | decision accuracy | evidence coverage |
|---:|---:|---:|---:|---:|---:|
| 360 | 0.295 | 0.505 | **0.351** | **0.750** | **0.725** |
| 720 | 0.295 | **0.533** | 0.340 | 0.375 | 0.113 |

`checkpoint-720` 的静态 loss 更低，但 decision 经常误用 compose 任务的 `canonical_rules` finish 格式，导致最终决策大幅变差。这说明多任务 Agent 不能只用 teacher-forcing loss 选轮次，也不能在 system prompt 中同时展示多个任务的结束格式。

## 修正版正式 SFT

修正版为三个任务分别构造 system prompt，只展示当前任务唯一合法的 finish schema；训练仍使用全部 2880 条轨迹。考虑到旧版第 3 轮已经出现验证 loss 反弹，本次训练 2 轮，共 720 step：

| 轮次 | checkpoint | eval loss | eval token accuracy |
|---:|---:|---:|---:|
| 1 | 360 | 0.02270 | 0.99237 |
| 2 | 720 | **0.01923** | **0.99421** |

- 总运行时间：42 分 8 秒。
- 峰值 HBM：190.01 GiB。
- 第二轮静态验证与动态验证都优于第一轮，因此修正版 GRPO 使用 `checkpoint-720`。

### 修正版 120 条真实动态轨迹

每个任务 40 条，`checkpoint-720` 使用最多 6 轮：专家最短路径仍是 retrieve/compose 3 轮、decision 4 轮，额外两轮只用于从一次错误动作恢复。

| 任务 | 关键阶段指标 | 协议分 | task schema | 反思成功率 |
|---|---:|---:|---:|---:|
| retrieve | 检索 F1 0.519 | 1.000 | 0.875 | 0.250 |
| compose | 组合 F1 **0.818** | 1.000 | 1.000 | **0.850** |
| decision | accuracy **0.950**；组合 F1 0.780；证据覆盖 0.717 | 0.996 | 0.975 | 0.575 |

复核保存轨迹时还发现，旧协议把纯空白 `<think>\n\n</think>` 误算成满协议分；旧独立
评测器也没有给 `InferRequest` 传 `enable_thinking=true`，Qwen3.5 混合模板因此主动预填
空思考块。旧 SFT 动态评测里 478 个动作的非空覆盖率确实是 0，但这是“评测关闭思考”的
直接结果，不能反推模型没有学到 SFT 里的规划文本。SFT 和 GRPO 训练脚本从一开始就明确
开启了思考，当前长跑的 rollout 没有这个评测配置错误；不过它启动时的旧协议奖励尚未
单独惩罚空思考，因此仍不能把事后修正冒充训练时已经生效。

课程现同时修复两层：环境要求 `<think>` 至少包含一个非空字符并记录 `thinking_score`；
动态评测默认开启 Qwen3.5 思考，并把该开关加入协议指纹。训练结束后的统一评测会给出
可比较的真实覆盖率。若覆盖率仍明显不足，应把它作为可复现的协议退化，而不是用动作
正确率掩盖。

进一步用 `audit_supervision.py` 把每个任务前 4 条、共 12 条真实 SFT 轨迹送入
ms-swift 的 Qwen3.5 训练模板：retrieve、compose、decision 分别有 12、12、16 个
assistant 目标，原始数据和编码后 loss mask 中的非空 `<think>` 数完全一致，失败记录为
0；监督 token 占完整输入的比例分别为 9.29%、6.17%、11.51%。因此“思考出现在 JSON”
和“思考真正参与 SFT 损失”两层都已有独立证据。

另一个训练后审计发现，环境虽然允许连续多次 `reflect`，旧指标却会让最后一次改写覆盖
此前成功增益。课程现同时保留 `reflection_gain`（最近一次）和
`reflection_best_gain`（历史最佳），并让反思奖励读取后者；单测覆盖“先修正成功、再改写
变差”后仍保留成功信用。本次已经启动的长跑仍使用旧的末次增益奖励，严格评测使用新
指标做轨迹分析；这个修复不会被追写成训练时已经生效。

旧版最大 4 轮时，第二轮 SFT 的 decision accuracy 只有 0.10。轨迹显示模型常在第一步错误调用 `reflect`，随后虽然能完成 search、reflect、compose，却没有剩余轮次 finish。把上限增到 6 并没有改变最短专家路径，而是把“任务能力”和“一次动作失误后的可恢复性”分开评测；相同 checkpoint 的 accuracy 随之恢复到 0.95。

## 正式 GRPO

### 第一版多任务运行的失败诊断

旧版首次满数据运行在 146/1440 step 主动停止，而不是隐藏失败继续耗时。前 100 步 reward 均值约 2.13、decision reward 约 0.84，但 121～140 步的 composition reward 已降到 0。轨迹审计发现：

- compose 任务开始输出 decision 的 `decision/matched_rules/evidence` 结束格式。
- 旧 system prompt 同时展示三种 finish schema，放大了多任务串线。
- 旧协议奖励只检查 JSON 外壳，错任务 schema 仍可得高分。
- G=2 时两个错误输出完全相同会产生零优势，无法自我修复。

修正版只在 system prompt 中展示当前任务唯一合法的 finish schema，环境新增 `invalid_finish_schema` 和 `task_schema_score`，协议奖励也显式计入 schema 正确性。

### 修正版正式运行

已使用 2880 条全量三任务数据启动 2 轮训练，共 2880 step；参数为 batch 6、G=3、generation batch 12、temperature 0.8、每轮最多 160 token、最多 6 轮，并开启梯度检查点。平台执行会话在一次长跑中发出过外部 `SIGTERM`，因此训练迁入持久 `tmux` 会话，保存间隔由 240 缩短为 120 step，最多保留 24 个带优化器状态的可恢复检查点。首次恢复还发现 Transformers 会从旧 `trainer_state.json` 沿用 240-step 间隔；脚本现已在恢复副本中同步该字段，不修改原检查点。480-step 完整优化器在同一 ROCm 节点恢复时，Accelerate 仍把 Adam 动量重映射成 FP32，随后 BF16 梯度导致第一次 `optimizer.step()` 报 dtype 不一致；使用 `RESUME_RESET_OPTIMIZER=true` 保留模型、步数、随机状态和数据位置，只重建动量与调度器后已成功继续。

训练后的动态评测预先固定两级协议：前 120 条验证轨迹（40 篇新闻、每篇三个任务）只负责在各 checkpoint 中选模型；剩余 840 条轨迹（另外 280 篇新闻）只在选择完成后比较 SFT 与最佳 GRPO。选择分数对 retrieve、compose、decision 三个任务等权，decision 子分数再平均类别正确率、规则 F1 和证据覆盖率。这能避免既按同一小批验证样本挑 checkpoint，又把它当作最终泛化结果汇报。

720-step 里程碑已生成完整适配器、优化器、调度器、随机状态和 trainer state。下面按连续 60 个 rollout（当前生成复用配置约对应 120 个训练 step）统计训练内信号；step 区间只显示实际执行 rollout 的奇数 step：

| step 区间 | reward | decision reward | protocol reward | KL | 零方差组比例 |
|---:|---:|---:|---:|---:|---:|
| 1～119 | 1.403 | 0.690 | 0.628 | 0.180 | 0.125 |
| 121～239 | 2.604 | 0.947 | 0.968 | 0.099 | 0.404 |
| 241～359 | 2.604 | 0.930 | 0.984 | 0.121 | 0.450 |
| 361～479 | 2.579 | 0.923 | 0.987 | 0.132 | 0.496 |
| 481～599 | **2.648** | **0.945** | 0.988 | 0.184 | 0.592 |
| 601～719 | 2.368 | 0.752 | 0.957 | 0.411 | 0.487 |

前 600 step 的训练奖励先提升后保持稳定，最近一段则伴随 KL 增大而回落。训练 batch 的任务难度和采样轨迹会波动，所以这还不能证明泛化退化；它证明必须保留 480、720、960、1200、1440、1920、2160、2880 等阶段，并在相同动态验证子集上选模型，而不能只取最后一步。

### 601～720 step 超参数配对

原分支使用 `learning_rate=3e-6`、`beta=0.001`，在 600 step 后出现决策奖励下降和 KL 抬升。为区分随机 batch 波动与更新过猛，从共同祖先 `checkpoint-600` 恢复相同数据区间和随机状态，只把学习率降为 `1e-6`、KL 系数提高为 `0.01`，其他参数完全不变。两条分支都完成 120 step、60 个 rollout：

| 参数 | reward | decision reward | protocol reward | KL | 截断率 | 平均轮数 |
|---|---:|---:|---:|---:|---:|---:|
| `lr=3e-6, beta=0.001` | 2.368 | 0.752 | 0.957 | 0.411 | 0.00139 | 3.808 |
| `lr=1e-6, beta=0.01` | **2.486** | **0.849** | **0.978** | **0.240** | **0.00000** | **3.729** |
| `lr=1e-6, beta=0.01` 正式复跑 | **2.587** | **0.936** | **0.990** | **0.226** | **0.00000** | **3.681** |

差异在后 30 个 rollout 更明显：原参数 KL 为 0.532、decision reward 为 0.679，新参数分别为 0.261 和 0.788。检索与组合奖励基本持平，因此优势主要来自策略更新更稳定，而不是任务批次更容易。正式分支从同一 600-step 祖先独立复跑后仍保持优势，说明结论不是单次 vLLM 随机采样的偶然结果。课程默认值已切换为胜出参数；配对试验本身不混入正式曲线。

### 稳定参数第一轮曲线

稳定参数正式分支已经完成 `checkpoint-1440`，trainer state 中 `epoch=1.0`。适配器的 372 个 LoRA 张量和优化器的 1116 个张量均为有限值，权重、调度器、随机状态和 trainer state 文件齐全。下面每行严格包含连续 120 个训练 step、60 次新 rollout；中间复用同一 rollout 的 step 仍计入训练步时，但不重复计入奖励均值。

| step | reward | decision | retrieve | compose | protocol | KL | 截断率 | 平均轮数 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 601～720 | **2.587** | **0.936** | 0.896 | 0.922 | **0.990** | **0.226** | 0.0000 | 3.681 |
| 721～840 | 2.557 | 0.870 | 0.901 | 0.914 | 0.973 | 0.271 | 0.0000 | 3.810 |
| 841～960 | 2.431 | 0.814 | 0.922 | 0.928 | 0.969 | 0.281 | 0.0014 | 3.819 |
| 961～1080 | 2.413 | 0.779 | 0.927 | 0.904 | 0.963 | 0.269 | 0.0000 | 3.811 |
| 1081～1200 | 2.326 | 0.666 | 0.926 | **0.944** | 0.946 | 0.350 | 0.0028 | 3.917 |
| 1201～1320 | 2.417 | 0.771 | 0.920 | 0.935 | 0.965 | 0.358 | 0.0000 | 3.793 |
| 1321～1440 | 2.400 | 0.778 | **0.930** | 0.916 | 0.970 | 0.346 | 0.0000 | **3.754** |

第一轮内检索和组合奖励保持稳定，决策奖励在 1081～1200 达到低点后有所恢复，但仍低于 601～720；同时 KL 从 0.226 抬升到约 0.35。训练奖励受任务批次和在线采样影响，不能直接当作验证准确率，不过这个趋势进一步支持保留 720、960、1200 和 1440 并做固定选择集评测，而不是默认选择第一轮末尾。

### 稳定参数第二轮曲线

第二轮已完成至 2880 step。所有 checkpoint 权重与优化器张量均为有限值；“尖峰”列统计裁剪前 `loss>1` 或 `grad_norm>1000` 的 step 数，不能单独解释为更新已失控。

| step | reward | decision | retrieve | compose | protocol | KL | 截断率 | 平均轮数 | 尖峰 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1441～1560 | 2.422 | 0.781 | 0.898 | **0.945** | **0.967** | **0.381** | 0.0000 | 3.803 | 0 |
| 1561～1680 | **2.442** | **0.801** | 0.871 | 0.925 | 0.961 | 0.420 | 0.0000 | **3.758** | 0 |
| 1681～1800 | 2.356 | 0.751 | **0.899** | 0.891 | 0.959 | 0.431 | 0.0000 | 3.799 | 1 |
| 1801～1920 | 2.310 | 0.675 | 0.898 | 0.912 | 0.932 | 0.469 | 0.0028 | 3.981 | 3 |
| 1921～2040 | 2.339 | 0.711 | **0.929** | 0.919 | 0.953 | 0.437 | 0.0000 | **3.751** | 3 |
| 2041～2160 | 2.316 | 0.716 | 0.898 | 0.898 | 0.938 | 0.476 | 0.0000 | 3.839 | 0 |
| 2161～2280 | 2.343 | 0.726 | 0.926 | 0.918 | 0.954 | 0.438 | 0.0000 | 3.801 | 0 |
| 2281～2400 | 2.326 | 0.707 | 0.866 | 0.922 | 0.942 | 0.475 | 0.0000 | 3.840 | 1 |
| 2401～2520 | 2.331 | 0.722 | 0.912 | 0.905 | 0.947 | 0.470 | 0.0000 | 3.836 | 3 |
| 2521～2640 | 2.314 | 0.704 | 0.889 | **0.935** | 0.932 | 0.516 | 0.0000 | 3.835 | 0 |
| 2641～2760 | 2.319 | **0.729** | 0.856 | **0.940** | **0.946** | 0.489 | 0.0000 | **3.754** | 1 |
| 2761～2880 | 2.306 | **0.732** | **0.915** | 0.900 | **0.950** | **0.463** | 0.0000 | 3.756 | 1 |

1734 step 出现一次 `loss=7.63`、`grad_norm=33792` 的裁剪前尖峰，后续 step 立即恢复；2041～2280 没有 `loss>1` 或 `grad_norm>1000`。2281～2400 只在 2336 step 出现一次裁剪前 `grad_norm=1352`，当步 loss 仅 0.335。2401～2520 的 2430、2436、2467 step 梯度超过 1000，其中 2467 达到 `loss=2.38`、`grad_norm=19456`；下一步立即回到 loss 0.005、梯度 7.94，2520 checkpoint 的 372 个 LoRA 张量与 1116 个优化器张量也全部有限。更值得关注的是窗口层面的共同变化：决策和协议奖励仍低于第二轮早段，KL 维持在约 0.44～0.48。它可能表示在线策略开始偏离稳定区域，也可能受第二轮当前任务批次影响；因此后半轮每 120 step 都进入正式动态评测候选，但在固定选择集结果出来前不把训练内信号写成泛化结论。

训练在日志写到 2562、但尚未保存下一检查点时被外部执行环境终止；控制台没有 traceback、OOM 或训练器主动退出记录，因此只认可已完整落盘并校验的 checkpoint-2520。恢复运行保留 adapter、全局步数、随机状态和数据位置；为规避本机此前实测的 ROCm/BF16 Adam 状态重映射问题，重建优化器和调度器，并把基础学习率显式设成中断时的低量级 `2.3e-7`，没有跳回 `1e-6`。这一做法会让恢复段调度曲线与原连续运行略有差异，所以 checkpoint-2520 仍保留在正式候选中，不能把续训最后一步默认写成最佳模型。

恢复后的 2521～2640 共 120 step、60 次新 rollout，端到端平均 11.40 秒/step；最大 loss 0.202、最大裁剪前梯度 612，非有限值、`loss>1` 和 `grad_norm>1000` 均为 0。checkpoint-2640 的 372 个 LoRA 张量与 1116 个优化器张量全部有限，trainer state 为 epoch 1.8333、global step 2640。该窗口训练 reward 与 decision reward 略低于 2401～2520，组合奖励更高、KL 升至 0.516；这些仍是在线训练批次信号，是否优于 2520 只能由固定选择集决定。

2641～2760 的端到端平均步时进一步降至 10.38 秒；2749 step 出现一次 `loss=1.65`、裁剪前 `grad_norm=9792` 的孤立尖峰，2750 立即恢复为 loss 0.0097、梯度 7.38，其余 step 没有非有限值。checkpoint-2760 的 372 个 LoRA 张量和 1116 个优化器张量仍全部有限，trainer state 为 epoch 1.9167。该窗口的 decision、composition、protocol 和 KL 均比前一窗口略好，但 retrieve 较低；同样只作为训练健康记录，不替代固定选择集评测。

最终 2761～2880 窗口平均 10.04 秒/step，2875 step 只有一次 `loss=0.306`、裁剪前 `grad_norm=1080` 的轻微尖峰，没有非有限值或 `loss>1`。恢复运行本身耗时 1 小时 5 分 31 秒并正常退出，最终 checkpoint-2880 的 trainer state 为 epoch 2.0、global step 2880；adapter 和 optimizer 张量继续全部有限。因此“两轮全数据训练完成”已有文件、状态和数值三层证据，但哪个 checkpoint 最好仍等待选择集与留出集结果。

## 复核命令

```bash
python course/25_agent_r1_news/evaluate_pipeline.py
python course/25_agent_r1_news/summarize_training.py outputs/25_agent_r1_news/某次运行/logging.jsonl
```
